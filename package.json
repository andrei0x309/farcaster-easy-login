{
  "name": "warp-easy-login-browser-extension",
  "description": "This extension allows you to sign in to warpcast.com without having to use a mobile device.",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "compose-inject": "yarn tsx ./scripts/compose-inject.ts",
    "compile-inject": "tsc --outFile ./src/client/inject.js ./src/client/inject-composed.ts",
    "dev": "vite",
    "build": "yarn compose-inject && yarn compile-inject && vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "pretty": "prettier --write .",
    "release": "yarn config set version-tag-prefix warp-easy-login-browser-extension@v &&  yarn config set version-git-message 'warp-easy-login-browser-extension@v%s' && yarn version --patch && yarn postversion",
    "postversion": "git push",
    "create-release": "tsx ./scripts/create-release.ts",
    "pub": "yarn build && yarn release && yarn create-release"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.23",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tailwindcss/vite": "^4.1.11",
    "@tsconfig/svelte": "^5.0.2",
    "@types/chrome": "^0.0.263",
    "archiver": "^7.0.1",
    "prettier": "^3.2.5",
    "sass": "^1.58.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tailwindcss": "^4.1.11",
    "tslib": "^2.6.2",
    "typescript": "^5.5.0",
    "vite": "^5.4.4"
  }
}
